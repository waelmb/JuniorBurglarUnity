using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemList : MonoBehaviour
{
    public static bool[] RetirementHomeItemList = new bool[]{false, false, false, false, false, false};
    public static bool[] RestaurantItemList = new bool[]{false, false, false, false, false};
    public static bool[] PlaygroundItemList = new bool[]{false, false, false, false, false};
    public static bool[] ParkItemList = new bool[]{false, false, false, false, false};
    public static bool[] BankItemList = new bool[]{false, false, false, false, false, false, false, false, false};
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
